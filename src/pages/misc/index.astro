---
import GradientHeader from '../../components/gradientHeader.svelte';
import Layout from '../../layouts/Layout.astro'
const mdPages = await Astro.glob('./**/*.md')
---

<Layout title="Misc">
	<GradientHeader title="Miscellaneous Pages" client:load />
	<hr>
	<h3 class="text-center p-4 dark:text-white">
		These pages are likely of use to nobody, however, I deemed them worthy (or they needed) to be hosted on a website. 
	</h3>
	<hr>
	<h3 class="text-center p-4">
		{mdPages.map((page) => (<a href={page.url}>{page.url}</a>))}
	</h3>
</Layout>